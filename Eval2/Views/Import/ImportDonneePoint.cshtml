@{
    if (User.IsInRole("Admin"))
    {
        Layout = "_Admin";
    }
}
<h3>Import Point</h3>
<form method="post" enctype="multipart/form-data" asp-controller="Import" asp-action="importerPoint">
    <div class="form-group">
        <label for="fichier1">Sélectionner le premier fichier CSV :</label>
        <input type="file" class="form-control-file" id="fichier1" name="fichier" accept=".csv">
    </div>
    <input type="submit" class="btn btn-primary" value="Importer">

</form>

@if (ViewBag.Message != null)
{
    <div class="alert alert-info">@ViewBag.Message</div>
}
